apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: planes-test-globe-history
spec:
  storageClassName: nfs-provisioner
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
