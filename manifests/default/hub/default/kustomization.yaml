resources:
  - ../base
namePrefix: hub-
patches:
  - target:
      kind: StatefulSet
    patch: |-
      - op: add
        path: /spec/template/spec/containers/0/env/-
        value:
          name: READSB_NET_CONNECTOR
          value: "planes-readsb,30004,beast_reduce_out"
