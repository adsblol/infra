resources:
  - ../base
namePrefix: ingest-
commonLabels:
  service: public
# Patch:
# add ENV
patches:
  - target:
      kind: StatefulSet
    patch: |-
      - op: add
        path: /spec/template/spec/containers/0/env/-
        value:
          name: READSB_NET_CONNECTOR
          value: "hub-readsb,30004,beast_out"
      - op: add
        path: /spec/template/spec/containers/0/env/-
        value:
         name: READSB_EXTRA_ARGS
         value: "--debug=nS"
